package com.company;

import java.io.*;

/**
 * Created by  Sapir Michaeli on 27.03.2017.
 */
public class ReadFile {
    /*int key;
    File file, encOrDecFile;
    boolean isChoiseEncrypt = false;
    InputStream inputStream = null;
    OutputStream outputStream = null;
    try{
        inputStream = new FileInputStream(file);

        outputStream = new FileOutputStream(encOrDecFile);
        int actuallyRead = 0;
        byte currentByte;
        while ((actuallyRead = inputStream.read()) != -1) {
            if (isChoiseEncrypt) {
                currentByte = doEncrypt(encryptionType, actuallyRead, key);
            } else
                currentByte = doDecrypt(encryptionType, actuallyRead, key);
            outputStream.write(currentByte);
        }
    } catch(FileNotFoundException e1){
        e1.printStackTrace();
    } catch(Exception e){
        e.printStackTrace();
    } finally

    {
        if (inputStream != null)
            try {
                inputStream.close();
            } catch (IOException e) {
                e.printStackTrace();
            }
        if (outputStream != null)
            try {
                outputStream.close();
            } catch (IOException e) {
                e.printStackTrace();
            }
    }*/
}
